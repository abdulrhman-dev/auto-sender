
services:
  waha:
    image: devlikeapro/waha:latest
    container_name: waha
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      # This keeps your WhatsApp session data persistent
      - ./sessions:/sessions
    restart: unless-stopped
